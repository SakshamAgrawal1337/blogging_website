const WelcomeMessage = ({onGetPostClick}) =>{
    return  (<center className="Welcome-Msg">
        <h1 > No Post Yet!, Add some posts to see them here. </h1>

</center>);
};

export default WelcomeMessage;